# qa-kit increment 3 — `/qa-spec` + spec snapshot Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.
>
> **⚠️ PROVISIONAL DEPENDENCY:** this plan consumes **increment 1's constitution version/state format** — the fenced ```json `{roles:[{id,role,plane}], version}` block in `.qa/constitution.md` and the `constitution.sh version` digest. That format is *specified* (design §3, increment-1 plan Task 1) but only *built* in increment 1. **Before executing this plan, confirm increment 1 landed and re-pin the exact state-block location/shape** (`.qa/constitution.md` fenced block vs a sibling `.qa/constitution.state.json`) — the code below assumes the fenced-block-in-constitution.md form; adjust if increment 1 chose the sibling file.

**Goal:** Build `/qa-spec` — the second qa-kit step — which authors a per-target QA spec that **pins an immutable role snapshot** (`spec-roles.json`) copied from the constitution + stamped with its version, applies per-spec overrides + a **run-config** override section (the folded-in `/qa-plan`, R2-Q3), and surfaces a **drift advisory** when its stamped version ≠ the current constitution's. One spec → N runs (R2-Q5).

**Architecture:** `/qa-spec` reuses `detecting-stack-profile` (stack facts) + optionally `ingesting-spec-kit` (seed oracles/criteria from a *feature* spec-kit) and writes `.qa/specs/<target>/{qa-spec.md, spec-roles.json}`. The genuinely-new logic is `spec-snapshot.sh` (pure, dual-engine): copy the constitution's roles into `spec-roles.json`, stamp the constitution version, apply overrides, and diff a stamped version against the current constitution version. Roles are soft while authoring; they freeze at `plan_frozen` (ADR-0020) when a run starts (increment 5 wires that) — this increment authors the snapshot the run will freeze.

## Global Constraints

- **Snapshot = copy, not reference (design decision 6).** `spec-roles.json` is a point-in-time copy + a stamped `constitutionVersion`. A run replays it; it never re-reads the live constitution mid-run.
- **Reuse existing role state.** The source is `.qa/config.json` `personas[]` (`[{id,role,plane∈global|contextual,auth}]`); the canonical snapshot tuple is `id|role|plane` (auth EXCLUDED — matches increment 1's version canonicalization). Never re-run discovery here.
- **Run-config is per-spec overrides of `.qa/config.json` defaults** (drivers/`maxParallel`/`criteriaBudget`/`viewport`), NOT a re-statement — store only the deltas the spec changes. **This is "run-config," not "plan"** (R2 terminology).
- **Deterministic + dual-engine** (`jq`/`python3`, honor `QA_ENGINE`, die-if-neither); no `grep -P`/`perl`/`node`; no attribution; never commit `dist/`.
- **Drift is an advisory, never an auto-migrate** (R2-Q6): a stale stamp prints a warning; it does not rewrite the snapshot.

## File Structure

- `skills/checkpointing-qa-memory/scripts/spec-snapshot.sh` **(new)** — `create` / `drift` (pure, dual-engine).
- `tests/spec-snapshot/run.sh` **(new)** — dual-engine tests.
- `core/qa-kit/qa-spec.command.md` **(new, staged)** — the `/qa-spec` command body (wired by increment 2's packaging).
- `core/qa-kit/qa-spec-template.md` **(new)** — the `qa-spec.md` skeleton (scenario/role selections, run-config, oracles, out-of-scope).

## Task 1: `spec-snapshot.sh` — copy + stamp + overrides + drift

**Interfaces:**
- `spec-snapshot.sh create <config.json> <constitution-state> <spec-dir> [<overrides.json>]` → reads `personas[]` from `<config.json>`, the current version from `<constitution-state>` (the fenced-json block extracted from `.qa/constitution.md`, or the sibling state file — see the provisional note), applies `<overrides.json>` (optional: `{subset:[ids], add:[{id,role,plane}], modify:[{id, role?, plane?}]}`), and writes `<spec-dir>/spec-roles.json` = `{constitutionVersion:<v>, roles:[{id,role,plane}], overrides:<the applied overrides|null>}`. `subset` filters to the listed ids; `modify` patches role/plane on a matching id; `add` appends (die on an add-id colliding an existing id). Deterministic.
- `spec-snapshot.sh drift <spec-roles.json> <current-constitution-version>` → prints `{stale:<bool>, stamped:<v>, current:<v>}`; `stale` = the two versions differ.

- [ ] **Step 1: Write the failing tests** (`tests/spec-snapshot/run.sh`, dual-engine, mirroring `tests/constitution/run.sh`):
  - `create` with no overrides → `spec-roles.json` has the same role ids as `config.json` personas (as `{id,role,plane}`, auth dropped) + the stamped `constitutionVersion`.
  - `create` with `{subset:["admin"]}` → only `admin` in the snapshot.
  - `create` with `{add:[{id:"bot",role:"bot",plane:"global"}]}` → `bot` present; adding an id that already exists → non-zero (die).
  - `create` with `{modify:[{id:"admin",plane:"contextual"}]}` → admin's plane patched, role unchanged.
  - `drift`: stamped == current → `stale:false`; stamped != current → `stale:true` with both values.
  - dual-engine byte-identical `spec-roles.json`.
- [ ] **Step 2:** run → FAIL (script missing).
- [ ] **Step 3: Implement `spec-snapshot.sh`** — jq/py/die header (copy the sibling idiom). `create`: extract `personas` → map to `{id,role,plane}`, apply subset→modify→add in that order, read the constitution version from the state input, write the snapshot object (sorted by id for determinism). `drift`: compare the stamped `constitutionVersion` to the passed current version.
- [ ] **Step 4:** run → `FAIL=0`; `bash -n`.
- [ ] **Step 5: Commit** `feat(qa-kit): spec-snapshot.sh — copy+stamp constitution roles into spec-roles.json (overrides + drift)`

## Task 2: `/qa-spec` command body + template (staged)

**Files (staged under `core/qa-kit/`, wired by increment 2):** `core/qa-kit/qa-spec.command.md`, `core/qa-kit/qa-spec-template.md`.

- [ ] **Step 1: Template** — `qa-spec.md` skeleton: `## Target`, `## Scenario selection` (which scenarios/features this run covers), `## Roles` (which of the constitution's roles + per-spec overrides), `## Run-config` (drivers/budget deltas vs `.qa/config.json`), `## Oracles & out-of-scope` (what "correct" means; what's explicitly not tested), `## Ingested spec-kit` (optional, when a feature `spec.md`/`tasks.md` was ingested).
- [ ] **Step 2: Command body** (`qa-spec.command.md`, imperative) — prereq check: `.qa/constitution.md` should exist; if not, per design decision 10, offer to bootstrap (`/qa-constitution`) OR proceed with per-spec-only roles. Then: (1) run `detecting-stack-profile`; (2) optionally `ingesting-spec-kit` if a feature spec-kit is present; (3) author the role selection + overrides → call `spec-snapshot.sh create` → `spec-roles.json`; (4) run `spec-snapshot.sh drift` and print any advisory; (5) write `qa-spec.md` from the template. State plainly: roles are frozen when a run starts (`plan_frozen`), not now.
- [ ] **Step 3: Gate (no build-wiring, R2-Q7)** — `build-adapter.sh claude` + `validate-adapters.sh` exit 0 (confirm nothing broke; staged files aren't shipped this increment). Re-run `tests/spec-snapshot/run.sh`.
- [ ] **Step 4: Commit** `feat(qa-kit): /qa-spec command body + template (staged; pins spec-roles snapshot + run-config + drift advisory)`

## Task 3: docs

- [ ] **Step 1:** append to ADR-0022 an implementation note (increment 3 landed: `/qa-spec` + `spec-roles.json` snapshot semantics + run-config folded in + drift advisory). Update the design/roadmap status lines for increment 3.
- [ ] **Step 2: Commit** `docs(qa-kit): ADR-0022 note — increment 3 (/qa-spec snapshot + run-config + drift) landed`

## Self-Review

**1. Coverage (design §3 spec row + §4 state model + R2-Q3/Q5/Q6):** snapshot-copy + version stamp → Task 1 `create`; overrides + run-config → Task 1 overrides + Task 2 template; drift advisory → Task 1 `drift` + Task 2 step 4; 1-spec-N-runs → the snapshot is authored once, frozen per-run (increment 5). ✅ Freeze-at-`plan_frozen` itself is increment 5 (run wiring) — noted, not built here.

**2. Placeholder scan:** the only provisional is the constitution-state *location* (fenced-block vs sibling file), flagged at the top with a pin-before-execute instruction — the rest (personas shape, snapshot shape, overrides semantics, drift) is concrete.

**3. Type consistency:** the snapshot tuple `{id,role,plane}` matches increment 1's canonical tuple (auth excluded); `constitutionVersion` is exactly increment 1's `constitution.sh version` output; `personas[]` `{id,role,plane,auth}` matches `write-persona-config.sh`. Vocabulary: "run-config" (not "plan"), "step" (not "phase").

## Execution Handoff

SDD. **Depends on increment 1** (constitution version format). Pin the constitution-state location before Task 1 (provisional note). Next: increment 4 (`/qa-scenarios` + `/qa-analyze` + enforcement seam).
