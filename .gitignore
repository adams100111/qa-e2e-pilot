# Per-project QA runtime — only the .example is tracked
.qa/config.json
.qa/runs/
.qa/auth/

# Node / npx installer
node_modules/
*.log
__pycache__/
*.py[cod]

# OS
.DS_Store

# Playwright MCP session scratch
.playwright-mcp/

# graft code-index per-session cache
graft/

# Multi-harness adapter build output (scripts/build-adapter.sh) — regenerated, never committed
dist/
qa-kit/dist/
.superpowers/
outputs/skillopt/
