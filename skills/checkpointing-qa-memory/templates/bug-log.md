{
  "run_id": "{{RUN_ID}}",
  "entries": [
    {
      "bug_id": "{{BUG_ID}}",
      "criterion_id": "{{CRITERION_ID}}",
      "title": "{{TITLE}}",
      "severity": "{{SEVERITY}}",
      "suspected_layer": "{{SUSPECTED_LAYER}}",
      "steps": [
        "{{STEP_1}}",
        "{{STEP_2}}"
      ],
      "expected": "{{EXPECTED}}",
      "actual": "{{ACTUAL}}",
      "evidence_refs": [
        "evidence/{{CRITERION_ID}}/screenshot-after.png"
      ],
      "fix": "{{FIX_NOTES_OR_NULL}}",
      "logged_at": "{{LOGGED_AT}}"
    }
  ]
}
