{
  "run_id": "{{RUN_ID}}",
  "spec_kit_artifacts": {
    "spec": "{{SPEC_FILE_PATH_OR_NULL}}",
    "constitution": "{{CONSTITUTION_FILE_PATH_OR_NULL}}",
    "tasks": "{{TASKS_FILE_PATH_OR_NULL}}"
  },
  "rows": [
    {
      "criterion_id": "{{CRITERION_ID}}",
      "criterion_label": "{{CRITERION_LABEL}}",
      "spec_refs": ["{{SPEC_SECTION_ID}}"],
      "constitution_refs": ["{{CONSTITUTION_RULE_ID}}"],
      "task_refs": ["{{TASK_ID}}"],
      "verdict": null,
      "confidence": null
    }
  ]
}
