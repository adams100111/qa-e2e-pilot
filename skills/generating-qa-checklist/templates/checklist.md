# QA Checklist

<!-- COPY this file to .qa/runs/<run-id>/checklist.md and fill every field.
     Stop after filling; do not begin verification until a human has reviewed. -->

## Run Header

| Field | Value |
|---|---|
| Run ID | <!-- e.g. 2024-01-15-governance-01 --> |
| Feature | <!-- e.g. Governance module (cap table) --> |
| Base URL | <!-- e.g. https://staging.example.com --> |
| Surface map | <!-- .qa/runs/<run-id>/surface-map.json --> |
| Oracle sources | <!-- e.g. docs/constitution.md, spec.md, migration 2024_01_15_create_holdings_table.php --> |
| Spec-kit artifacts | <!-- constitution.md / spec.md / tasks.md — or "none" --> |
| Generated at | <!-- ISO-8601 --> |
| Generated by | <!-- generating-qa-checklist v1.1 / hand-authored / ingested from: --> |

---

## Criteria

<!-- One block per criterion. Duplicate this block as needed.
     ID format: C-<surface-short>-<sequence>  e.g. C-FINALIZE-01
     Tags: independent | read-only | race | cross-tenant | probe-needed
     (probe-needed: set when the expected state can't be confirmed via the visible UI alone —
      see generating-qa-checklist Step 6. probe-needed OR cross-tenant derives Kinds: probe.) -->

---

### C-EXAMPLE-01 — [Title: one behavior across all layers]

| Field | Value |
|---|---|
| Surface | <!-- route + sub-tab, e.g. /governance/setup → Finalize step --> |
| Kind | <!-- happy-path / multiplicity-0 / multiplicity-1 / multiplicity-N / empty-state / loading-state / error-state / computed-logic / business-rule / downstream-cascade / cross-tenant / race --> |
| Tags | <!-- independent / read-only / race / cross-tenant / probe-needed — or "sequential (default)" --> |
| Kinds | <!-- derived: bake / computed / probe (csv), per the Kind+Tags mapping table in generating-qa-checklist Step 6 — `probe` comes ONLY from Tag: cross-tenant or Tag: probe-needed (never guessed); read-only/pure-display with neither tag → none. Fed straight into `checkpoint.sh --kinds` at pass time. --> |
| probeNeeded | <!-- true if Kinds includes probe (cross-tenant / backend-probe criteria) — else false --> |
| Confidence hint | <!-- high — oracle from spec/domain rule; OR low — oracle derived from backend code only --> |
| Source | <!-- generated / ingested / hand-authored --> |

**Steps**

1. <!-- Drive: navigate to surface, perform the action. E.g. "Fill founder form; submit." -->
2. <!-- Bake: read-back path. E.g. "Navigate away, return to founders list; snapshot." -->
3. <!-- Recompute / reconcile (if computed logic): write the arithmetic here. -->
4. <!-- Downstream trace (if cascade): navigate to downstream entity and snapshot. -->
5. <!-- Cross-tenant sub-step (REQUIRED for every write): authenticate as Tenant B, read-back the same entity, assert ABSENT. -->

**EXPECTED (oracle — fill before verification begins)**

```
<!-- State the expected value or rule explicitly. Examples:
  - Exact value:    amount = 4,000,000 × $0.001 = $4,000.000 (tolerance: 0)
  - Shape:          holdings row exists; issued_at NOT NULL; shares NOT NULL; price_per_share = 0.001
  - Count:          3 founders visible in list; count badge = 3
  - Business rule:  finalize releases populated project view with ≥1 share class, ≥1 founder
  - Cross-tenant:   GET /api/founders as Tenant B → [] (empty, not the Tenant A row)
  - Race outcome:   second finalize returns 409 or is idempotent; no duplicate holding row
-->
```

**Baking assertion**

| Sub-field | Value |
|---|---|
| Entity | <!-- e.g. Holding, Founder, ShareClass --> |
| Read-back path | <!-- e.g. GET /api/holdings?project_id=X   OR   navigate to /governance/holdings list VIEW --> |
| Multiplicity expected | <!-- e.g. 0 before create; 1 after first create; N=3 after wizard finalize --> |
| Required NOT-NULL fields | <!-- list every required/NOT-NULL column and its expected non-null value or type --> |
| Cross-tenant read-back | <!-- "as Tenant B → assert row ABSENT"  OR  "n/a (read-only criterion)" --> |

**Notes / bug risks**

<!-- Any known risk, edge, or prior bug class for this criterion. -->

---
<!-- REPEAT the block above for each criterion. Suggested order:
     1. Multiplicity-0 (empty-state) criteria FIRST — they are only valid before any create runs.
     2. Happy-path / multiplicity-1 criteria.
     3. Multiplicity-N criteria.
     4. Computed-logic and downstream-cascade criteria.
     5. Business-rule and gate criteria.
     6. Cross-tenant criteria (sequential; second-tenant auth).
     7. Race criteria (tagged race: true; fan out to second driver only).
     8. Error / loading / edge-state criteria last (can run independently).
-->

---

## Checklist Summary

| # | ID | Title | Kind | Tags | Kinds | Confidence | Status |
|---|---|---|---|---|---|---|---|
| 1 | C-EXAMPLE-01 | [title] | happy-path | sequential | bake | high | pending |
<!-- Add one row per criterion -->

---

## Human Review Sign-off

- [ ] Every criterion has an explicit EXPECTED (oracle) value or rule — no criterion left blank.
- [ ] Every write criterion has a baking assertion with NOT-NULL fields named.
- [ ] Every write criterion has a cross-tenant sub-step.
- [ ] Multiplicity-0 criteria are listed BEFORE any create criteria.
- [ ] Race criteria are tagged `race: true` and document the two-session scenario.
- [ ] All other criteria are tagged `sequential (default)` or explicitly `independent`/`read-only`.
- [ ] Oracle sources (spec, constitution.md, migration) are cited in the Run Header.
- [ ] Checklist reviewed and approved by: _________________ Date: _____________
